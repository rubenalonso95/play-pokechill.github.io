<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Fullscreen, no system bars: the whole screen belongs to the game. -->
    <style name="Theme.PokeChill" parent="@android:style/Theme.Material.NoActionBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>